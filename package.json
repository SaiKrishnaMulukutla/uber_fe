{
  "name": "uber_fe",
  "private": true,
  "scripts": {
    "dev:rider": "pnpm --filter rider dev",
    "dev:driver": "pnpm --filter driver dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
